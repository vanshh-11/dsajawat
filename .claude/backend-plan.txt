BACKEND IMPLEMENTATION PLAN - D SAJAWAT PORTFOLIO
====================================================

CURRENT STATE
-------------
- Pure React (CRA) frontend, NO backend
- Forms only open WhatsApp (window.open)
- All data hardcoded in src/types.ts
- No auth, no database, no API

REQUIRED BACKEND COMPONENTS
---------------------------

1. DATABASE (PostgreSQL via Supabase)
------------------------------------
-- Core tables needed
inquiries          -- Contact + Quote forms (source, status, notes)
products           -- Collections CMS (title, images, specs, pricing)
swatch_requests    -- Physical swatch kit orders
admin_users        -- Simple auth for dashboard

SQL SCHEMA:
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

CREATE TABLE inquiries (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  name TEXT NOT NULL,
  phone TEXT NOT NULL,
  event_type TEXT,
  message TEXT,
  source TEXT NOT NULL CHECK (source IN ('contact', 'quote', 'hero_cta', 'collection_card')),
  page_url TEXT,
  user_agent TEXT,
  referrer TEXT,
  status TEXT DEFAULT 'new' CHECK (status IN ('new', 'contacted', 'quoted', 'converted', 'spam', 'archived')),
  priority INTEGER DEFAULT 0,
  responded_at TIMESTAMPTZ,
  responded_by TEXT,
  notes TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE products (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  slug TEXT UNIQUE NOT NULL,
  title TEXT NOT NULL,
  overline TEXT,
  description TEXT,
  images TEXT[],
  category TEXT,
  gsm INTEGER,
  width_cm INTEGER,
  composition TEXT,
  colors JSONB,
  min_order_meters INTEGER,
  price_per_meter DECIMAL,
  featured BOOLEAN DEFAULT FALSE,
  active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE swatch_requests (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  inquiry_id UUID REFERENCES inquiries(id) ON DELETE SET NULL,
  name TEXT NOT NULL,
  phone TEXT NOT NULL,
  email TEXT,
  address JSONB NOT NULL,
  products_requested JSONB,
  tracking_number TEXT,
  status TEXT DEFAULT 'pending' CHECK (status IN ('pending', 'shipped', 'delivered', 'returned')),
  shipped_at TIMESTAMPTZ,
  delivered_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE admin_users (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  email TEXT UNIQUE NOT NULL,
  password_hash TEXT,
  magic_link_token TEXT,
  magic_link_expires TIMESTAMPTZ,
  role TEXT DEFAULT 'admin',
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Indexes
CREATE INDEX idx_inquiries_status ON inquiries(status);
CREATE INDEX idx_inquiries_created_at ON inquiries(created_at DESC);
CREATE INDEX idx_inquiries_source ON inquiries(source);
CREATE INDEX idx_inquiries_phone ON inquiries(phone);
CREATE INDEX idx_products_slug ON products(slug);
CREATE INDEX idx_products_category ON products(category);

-- Updated_at trigger
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = NOW();
  RETURN NEW;
END;
$$ language 'plpgsql';

CREATE TRIGGER update_inquiries_updated_at BEFORE UPDATE ON inquiries FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE TRIGGER update_products_updated_at BEFORE UPDATE ON products FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();


2. API ENDPOINTS (Vercel Functions)
-----------------------------------
File Structure:
api/
├── inquiries/
│   ├── create.ts        # POST - Submit form (Contact/Quote)
│   ├── list.ts          # GET - Admin: paginated list with filters
│   ├── get.ts           # GET - Admin: single inquiry detail
│   ├── update.ts        # PATCH - Admin: update status/notes
│   └── stats.ts         # GET - Admin: dashboard stats
├── products/
│   ├── list.ts          # GET - Public: fetch all collections
│   ├── get.ts           # GET - Public: single product detail
│   └── admin/
│       ├── create.ts    # POST - Admin: create product
│       ├── update.ts    # PATCH - Admin: update product
│       └── delete.ts    # DELETE - Admin: delete product
├── swatch/
│   ├── create.ts        # POST - Swatch kit request
│   └── list.ts          # GET - Admin: swatch orders
├── auth/
│   ├── login.ts         # POST - Admin login (magic link)
│   └── verify.ts        # GET - Verify magic link
└── utils/
    ├── supabase.ts      # Supabase client (server-side)
    ├── email.ts         # Resend email templates
    ├── validation.ts    # Zod schemas
    └── rate-limit.ts    # Upstash Redis rate limiter


3. EMAIL SERVICE (Resend)
-------------------------
Templates needed:
1. Admin Notification - New inquiry received (rich HTML with all fields, action buttons)
2. Customer Confirmation - "We received your inquiry, will reply in 24hrs"
3. Status Update - When admin changes status (quoted, converted)
4. Swatch Confirmation - Swatch kit shipped with tracking


4. ADMIN DASHBOARD (protected route /admin)
--------------------------------------------
- Inquiry table: filter by status/source/date
- Detail view: full form data + timeline
- Actions: mark contacted/quoted/converted, add notes
- Stats cards: new this week, conversion rate, avg response time


5. FILE STORAGE (Cloudinary / Supabase Storage)
------------------------------------------------
- Product images (collections)
- Swatch request uploads
- Auto-optimize + CDN


FRONTEND INTEGRATION POINTS
---------------------------
| Component          | Change                                                          |
|--------------------|-----------------------------------------------------------------|
| Contact.tsx        | Replace WhatsApp-only -> fetch('/api/inquiries') + fallback    |
| QuoteModal.tsx     | Same as above                                                   |
| Collections.tsx    | Fetch from '/api/products' instead of types.ts                 |
| New AdminDashboard | Protected route with inquiry management                        |


RECOMMENDED STACK (Zero-ops, Free Tier Friendly)
-------------------------------------------------
| Layer       | Service                    | Free Tier                    |
|-------------|----------------------------|------------------------------|
| Database    | Supabase (PostgreSQL)      | 500MB, 50k MAU               |
| API         | Vercel Functions           | 100GB-hours/mo               |
| Email       | Resend                     | 3k emails/mo                 |
| Storage     | Cloudinary                 | 25GB bandwidth               |
| Auth        | Supabase Auth (magic link) | Included                     |
| Deploy      | Vercel                     | Unlimited personal           |


IMPLEMENTATION PHASES
---------------------
| Phase | Scope                                           | Est. Effort |
|-------|-------------------------------------------------|-------------|
| 1     | DB schema + POST /inquiries + email            | 4-6 hrs     |
| 2     | Auth + dashboard (list/detail/update)          | 6-8 hrs     |
| 3     | Products API + image upload                    | 4-6 hrs     |
| 4     | Swatch request flow + tracking                 | 3-4 hrs     |
| 5     | Rate limiting, analytics, SEO                  | 2-3 hrs     |


CLARIFYING QUESTIONS
--------------------
1. Admin auth preference: Simple password (env var) or magic-link email?
2. Email domain: Use dsajawat@gmail.com via Resend's shared domain, or wait for custom domain?
3. Product pricing: Need price tiers (per meter, bulk discounts) in CMS?
4. WhatsApp integration: Keep as fallback after API submit? (Current plan: yes)
5. Timeline: Need MVP in 1 week, or phased over 2-3 weeks?


ENVIRONMENT VARIABLES
---------------------
# .env.local (local) + Vercel Dashboard (production)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIs... # Server-only!
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIs... # Client-safe (if needed)

RESEND_API_KEY=re_123456789
RESEND_FROM_EMAIL=D SAJAWAT <inquiries@dsajawat.com>
RESEND_ADMIN_EMAIL=dsajawat@gmail.com

# Optional: Upstash Redis for rate limiting
UPSTASH_REDIS_REST_URL=https://...
UPSTASH_REDIS_REST_TOKEN=...

# Admin
ADMIN_PASSWORD=your-secure-password # For basic auth (if using simple auth)
ADMIN_URL=https://dsajawat.com/admin

# Cloudinary
CLOUDINARY_CLOUD_NAME=...
CLOUDINARY_API_KEY=...
CLOUDINARY_API_SECRET=...